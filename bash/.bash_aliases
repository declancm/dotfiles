# The default .bashrc looks for this file and sources it so we
# can use it to add settings without modifying the .bashrc

alias vim='nvim'
alias tmux='tmux -2'

export EDITOR='nvim'

eval "$(zoxide init bash)"
